modules:

  - type: rpm-ostree
    install:
      # Needed for Pano clipboard.
      - libgda
      - libgda-sqlite

  - type: files
    files:
      - gnome/usr: /usr

  - type: script
    scripts:
      - custom-gnome-shell.sh

  - type: gnome-extensions
    install:
      - Brightness control using ddcutil
      #- Pano - Clipboard Manager # This is still not pushed to EGO.
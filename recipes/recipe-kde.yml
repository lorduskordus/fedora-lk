name: fedora-lk-kde
description: Custom Fedora KDE image based on uBlue, built with BlueBuild. (My personal, with NVIDIA support)

base-image: ghcr.io/lorduskordus/fedora-base-kde-nvidia
image-version: latest

modules:

  # Common
  - from-file: image_configs/common.yml
  
  # KDE
  #- from-file: image_configs/kde.yml

  # Temporarily include COSMIC
  - from-file: image_configs/include_cosmic.yml

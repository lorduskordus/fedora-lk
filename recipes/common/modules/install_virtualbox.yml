modules:

  - type: bling
    install:
      - rpmfusion

  - type: akmods
    base: coreos-stable
    install:
      - VirtualBox

  - type: rpm-ostree
    source: ghcr.io/lorduskordus/bluebuild-modules
    install:
      - VirtualBox
